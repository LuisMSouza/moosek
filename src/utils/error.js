/////////////////////// IMPORTS //////////////////////////

/////////////////////// SOURCE CODE ///////////////////////////
export default async (text, channel) => {
    await channel.send({ content: `\`❌ ${text}\`` });
}